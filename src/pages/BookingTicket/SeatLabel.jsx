const SeatLabel = ({ children }) => (
  <span className="text-xs text-white border-2 border-transparent rounded p-2 inline-block bg-transparent text-center">
    {children}
  </span>
);

export default SeatLabel;
